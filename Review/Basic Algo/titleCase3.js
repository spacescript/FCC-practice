function titleCase(str) {
    let words = str.split(" ");
    for(let i = 0; i < words.length; i++) {
        words[i].toLowerCase();
        words[i].replace()
    }
    return words[i];
}
  
const test = titleCase("I'm a little tea pot");
console.log(test);
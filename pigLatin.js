function translatePigLatin(str) {
    var conson;
    var regex = /[^aeiou]/;

    var arr = str.split('');

    while(arr[0] )

    return arr;
}
  
var test = translatePigLatin("glove");
console.log(test);